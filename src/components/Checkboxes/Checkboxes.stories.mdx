import { Meta, Story, Preview, Canvas } from "@storybook/addon-docs";
import { action } from "@storybook/addon-actions";
import { Checkbox, CHECK_BOX_TYPES } from "./Checkboxes";
import { useState } from "react";

<Meta title="Components/Checkboxes" component={Checkbox} />

# Radio Buttons

Radio buttons are used to select options within an application.

## Usage

Radio buttons are used to select options within an application.

<Preview withToolbar>
  <Story 
    name="Checkboxes"
    argTypes={{
      checked: {
        control: { type: 'boolean' },
      },
      disabled: {
        control: { type: 'boolean' },
      },
      label: {
        control: { type: 'text' },
      },
    }}
    args={{
      checked: false,
      disabled: false,
      label: "This is a radio button",
    }}>
    {(({ label, ...restProps }) => {
      return  (
        <Checkbox onChange={action('onChange')} {...restProps}>{ label }</Checkbox>
      )
    }).bind({})}
  </Story>
</Preview>

